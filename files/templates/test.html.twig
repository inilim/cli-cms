<!DOCTYPE html>
<html>
<head>
    <title>CLI-CMS with Twig</title>
</head>
<body>
    <h1>{{ title }}</h1>
    <p>{{ message }}</p>
    {% if items %}
    <ul>
    {% for item in items %}
        <li>{{ item }}</li>
    {% endfor %}
    </ul>
    {% endif %}
</body>
</html>